# 🚀 Инструкция: Пуш на GitHub и деплой на Render

## Шаг 1: Проверка Git репозитория

### Проверьте, инициализирован ли Git:

```bash
git status
```

Если увидите ошибку "not a git repository", значит нужно инициализировать:

```bash
git init
```

---

## Шаг 2: Проверка .gitignore

Убедитесь, что файл `.gitignore` содержит:

```
# Dependencies
node_modules/
.pnp
.pnp.js

# Testing
coverage/

# Production
dist/
build/

# Misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# OS
Thumbs.db
```

**Важно:** Файл `.env` должен быть в `.gitignore`, чтобы не залить токены в репозиторий!

---

## Шаг 3: Добавление файлов в Git

```bash
# Добавить все файлы
git add .

# Проверить, что будет закоммичено
git status
```

**Убедитесь, что НЕ добавляются:**
- ❌ `node_modules/`
- ❌ `.env` (если есть)
- ❌ `dist/`
- ✅ Все остальные файлы проекта

---

## Шаг 4: Первый коммит

```bash
git commit -m "Initial commit: сайт маникюрного кабинета"
```

---

## Шаг 5: Создание репозитория на GitHub

1. **Зайдите на [GitHub.com](https://github.com)** и войдите в аккаунт

2. **Создайте новый репозиторий:**
   - Нажмите "+" в правом верхнем углу → "New repository"
   - Или перейдите: https://github.com/new

3. **Настройте репозиторий:**
   ```
   Repository name: elena-manicure (или любое другое имя)
   Description: Сайт маникюрного кабинета Елены
   Visibility: Public (или Private, если хотите)
   ❌ НЕ ставьте галочки на "Initialize with README" и других опциях
   ```

4. **Нажмите "Create repository"**

5. **Скопируйте URL репозитория** (он будет показан на следующей странице)
   - Например: `https://github.com/ваш-username/elena-manicure.git`

---

## Шаг 6: Подключение локального репозитория к GitHub

```bash
# Добавить удаленный репозиторий (замените URL на ваш)
git remote add origin https://github.com/ваш-username/elena-manicure.git

# Проверить подключение
git remote -v
```

---

## Шаг 7: Пуш на GitHub

```bash
# Переименовать ветку в main (если нужно)
git branch -M main

# Отправить код на GitHub
git push -u origin main
```

Если попросит авторизацию:
- **Для HTTPS:** введите логин и Personal Access Token (не пароль!)
- **Для SSH:** настройте SSH ключи

---

## Шаг 8: Деплой на Render

### Вариант A: Через веб-интерфейс Render

1. **Зайдите на [Render.com](https://render.com)** и войдите

2. **Создайте новый Static Site:**
   - Нажмите "New +" → "Static Site"
   - Или: https://dashboard.render.com/new/static

3. **Подключите GitHub репозиторий:**
   - Нажмите "Connect account" (если еще не подключен GitHub)
   - Выберите ваш репозиторий `elena-manicure`
   - Нажмите "Connect"

4. **Настройте параметры:**
   ```
   Name: elena-manicure
   Branch: main
   Root Directory: (оставьте пустым)
   Build Command: npm install && npm run build
   Publish Directory: dist
   ```

5. **Добавьте переменные окружения:**
   - Нажмите "Advanced" → "Environment Variables"
   - Добавьте:
     ```
     VITE_TELEGRAM_BOT_TOKEN = ваш_токен_бота
     VITE_TELEGRAM_CHAT_ID = ваш_chat_id
     ```
   - **Важно:** Используйте значения из вашего `.env` файла

6. **Нажмите "Create Static Site"**

7. **Дождитесь деплоя** (2-5 минут)

8. **Ваш сайт будет доступен по адресу:**
   ```
   https://elena-manicure.onrender.com
   ```

---

### Вариант B: Через render.yaml (автоматически)

Если вы используете файл `render.yaml`:

1. **Убедитесь, что `render.yaml` есть в репозитории**

2. **На Render:**
   - "New +" → "Blueprint"
   - Подключите репозиторий
   - Render автоматически обнаружит `render.yaml`

3. **Добавьте переменные окружения вручную** (как в варианте A)

---

## Шаг 9: Автоматический деплой при обновлениях

После первого деплоя, при каждом `git push` на GitHub:
1. Render автоматически обнаружит изменения
2. Запустит новую сборку
3. Задеплоит обновленную версию

**Для обновления сайта просто:**
```bash
git add .
git commit -m "Описание изменений"
git push
```

---

## 🔐 Безопасность: Переменные окружения

### ❌ НЕ ДЕЛАЙТЕ:
- Не коммитьте файл `.env` в Git
- Не храните токены в коде

### ✅ ДЕЛАЙТЕ:
- Храните `.env` в `.gitignore`
- Добавляйте переменные окружения в настройках Render
- Используйте разные токены для разработки и продакшена

---

## 📝 Проверка после деплоя

1. **Откройте сайт** по адресу Render
2. **Проверьте:**
   - ✅ Сайт загружается
   - ✅ Иконки отображаются
   - ✅ Форма записи работает
   - ✅ Все изображения загружаются

3. **Проверьте консоль браузера** (F12) на ошибки

---

## 🐛 Решение проблем

### Ошибка при push:
```bash
# Если нужно обновить удаленный репозиторий
git pull origin main --allow-unrelated-histories
git push -u origin main
```

### Render не видит изменения:
- Проверьте, что вы пушите в правильную ветку (`main`)
- Проверьте логи сборки в панели Render

### Переменные окружения не работают:
- Убедитесь, что переменные начинаются с `VITE_`
- Пересоберите сайт после добавления переменных

---

## ✅ Готово!

Теперь ваш сайт:
- ✅ На GitHub
- ✅ Автоматически деплоится на Render при каждом push
- ✅ Доступен по адресу Render

**Следующие шаги:**
- Настройте кастомный домен (если нужно)
- Добавьте мониторинг
- Настройте резервное копирование

---

## 📚 Полезные ссылки

- [GitHub Docs](https://docs.github.com/)
- [Render Docs](https://render.com/docs)
- [Git Cheat Sheet](https://education.github.com/git-cheat-sheet-education.pdf)

