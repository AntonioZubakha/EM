# 🗺️ Роадмап миграции на бесплатную схему
## Frontend: GitHub Pages | Backend: Render (рекомендовано) / Railway / Fly.io

---

## 📋 Общая информация

**Текущее состояние:**
- Frontend: React + Vite (готов к деплою)
- Backend: Node.js + Express (использует файлы JSON)
- Хостинг: Render.com (платный после пробного периода)

**Целевое состояние (обновлено):**
- Frontend: GitHub Pages (бесплатно)
- Backend: Render (бесплатный план) + Supabase / Neon (бесплатная PostgreSQL)
- Альтернатива: Railway (кредиты $5/мес) или Fly.io
- Домен: elena-manicure.ru

**Важно:** 
- ⚠️ **Deta Cloud закрыт навсегда** — используем другие сервисы
- **Рекомендуемый вариант:** Render (без карты, бесплатно) + Supabase (бесплатная PostgreSQL)
- **Альтернативы:** Railway (простой деплой, но кредиты могут закончиться) или Fly.io
- Репозиторий: https://github.com/AntonioZubakha/EM
- Имя репозитория `EM` - нужно учесть при настройке `base` в `vite.config.ts`

---

## 🎯 Этап 1: Подготовка и анализ

### 1.1 Проверка текущего состояния
- [ ] Убедиться, что проект работает локально
- [ ] Проверить все переменные окружения
- [ ] Создать резервную копию данных (если есть на Render)
- [ ] Проверить, что все функции работают

### 1.2 Анализ зависимостей
- [ ] Проверить, какие npm пакеты используются в бэкенде
- [ ] Убедиться, что код готов к переходу с Deta на Supabase/Neon
- [ ] Проверить размер проекта (для бесплатных планов Render/Railway)
- [ ] Создать аккаунты Render и Supabase, подтвердить доступность из региона
  - Зайти на https://render.com и https://supabase.com, убедиться, что регистрация проходит
  - Для маникюрного кабинета задержка 100-200 мс не критична
  - Если Render недоступен, рассмотреть альтернативы (Railway с кредитами, Fly.io)

**Важные моменты:**
- **Render:** Бесплатный план, засыпает после 15 минут бездействия (пробуждение ~30 сек), карта не нужна
- **Railway:** $5 бесплатных кредитов/месяц, поддержка Node.js/PostgreSQL, может потребоваться карта
- **Fly.io:** Бесплатный план с ограничениями, хорошая производительность
- **Supabase:** Бесплатная PostgreSQL база (500MB, 2GB bandwidth), REST API
- **Neon:** Бесплатная PostgreSQL база (3GB storage), serverless

---

## 🔧 Этап 2: Адаптация бэкенда под облачное хранилище

### 2.1 Выбор платформы

**Вариант 1: Render + Supabase (РЕКОМЕНДУЕТСЯ)**
- Render: бесплатный план, засыпает после 15 минут бездействия (пробуждение ~30 сек), карта не нужна
- Supabase: бесплатная PostgreSQL база данных
- Преимущества: полностью бесплатно и просто

**Вариант 2: Railway + Supabase**
- Railway: простой деплой Node.js, $5 бесплатных кредитов/месяц, возможен запрос карты
- Supabase: бесплатная PostgreSQL база данных
- Преимущества: быстрее ответ (нет сна), но кредит может закончиться

**Вариант 3: Fly.io + Supabase**
- Fly.io: бесплатный план с ограничениями
- Supabase: бесплатная PostgreSQL база данных
- Преимущества: хорошая производительность, но сложнее настройка

### 2.2 Установка зависимостей

**Для Supabase (PostgreSQL):**
```bash
cd server
npm install @supabase/supabase-js pg
```

**Альтернатива - использовать файлы на Railway/Render:**
- Можно оставить текущую структуру с JSON файлами
- Файлы будут храниться в файловой системе сервера
- Подходит для небольших объемов данных

### 2.3 Создание модуля для работы с хранилищем

**Вариант A: Supabase (PostgreSQL) - РЕКОМЕНДУЕТСЯ**

**Файл: `server/dbStorage.js`**
- [ ] Создать модуль для работы с Supabase PostgreSQL
- [ ] Реализовать функции:
  - `loadBookedSlots()` → чтение из PostgreSQL
  - `saveBookedSlot(slot)` → запись одного слота
  - `saveBookedSlots(slots)` → запись массива слотов
  - `deleteBookedSlot(date, time)` → удаление слота
  - `loadWorkingDays()` → чтение из PostgreSQL
  - `saveWorkingDay(date, status)` → запись одного дня
  - `deleteWorkingDay(date)` → удаление переопределения дня

**Структура таблиц PostgreSQL:**
```sql
-- Таблица booked_slots
CREATE TABLE booked_slots (
  id SERIAL PRIMARY KEY,
  date VARCHAR(10) NOT NULL,
  time VARCHAR(5) NOT NULL,
  name VARCHAR(100),
  phone VARCHAR(20),
  service VARCHAR(200),
  booked_at TIMESTAMP DEFAULT NOW(),
  UNIQUE(date, time)
);

-- Таблица working_days
CREATE TABLE working_days (
  date VARCHAR(10) PRIMARY KEY,
  status VARCHAR(10) NOT NULL CHECK (status IN ('working', 'off'))
);
```

**Вариант B: Файловая система (проще, но менее надежно)**
- Оставить текущую структуру с JSON файлами
- Файлы хранятся в файловой системе сервера
- Подходит для Railway/Render/Fly.io

**Пример миграции структуры:**
```javascript
// Старая структура (JSON файл):
{
  "bookedSlots": [
    { "date": "2025-01-15", "time": "14:00", "name": "Иван", ... },
    { "date": "2025-01-15", "time": "14:30", "name": "Иван", ... }
  ]
}

// Новая структура (Supabase):
// Строка 1: { date: "2025-01-15", time: "14:00", name: "Иван", ... }
// Строка 2: { date: "2025-01-15", time: "14:30", name: "Иван", ... }
```

**Реализация loadBookedSlots() с пагинацией (Supabase):**
```javascript
async function loadBookedSlots() {
  try {
    const PAGE_SIZE = 1000;
    let from = 0;
    let to = PAGE_SIZE - 1;
    const allSlots = [];

    while (true) {
      const { data, error } = await supabase
        .from('booked_slots')
        .select('*')
        .range(from, to);

      if (error) throw error;
      allSlots.push(...(data || []));

      if (!data || data.length < PAGE_SIZE) break;
      from += PAGE_SIZE;
      to += PAGE_SIZE;
    }

    return allSlots;
  } catch (error) {
    console.error('Ошибка при загрузке слотов из Supabase:', error);
    return [];
  }
}
```

### 2.4 Адаптация `server/index.js`

**Если используем Supabase:**
- [ ] Заменить импорты `fs` на `dbStorage` (Supabase)
- [ ] Обновить все функции для работы с PostgreSQL
- [ ] Убрать работу с файлами

**Если используем файловую систему:**
- [ ] Оставить текущую структуру с `fs`
- [ ] Файлы будут работать на Railway/Render/Fly.io
- [ ] Убедиться, что папка `data` создается автоматически

### 2.4 Обработка ошибок
- [ ] Добавить обработку ошибок Supabase API
- [ ] Добавить fallback/ретраи на случай недоступности БД
- [ ] Логирование ошибок

### 2.5 Настройка CORS для GitHub Pages домена
- [ ] Добавить middleware для CORS в `server/index.js`
- [ ] Разрешить домен фронтенда (elena-manicure.ru, www.elena-manicure.ru)
- [ ] Разрешить методы: GET, POST, DELETE, OPTIONS
- [ ] Настроить заголовки: Content-Type, Authorization
- [ ] Протестировать CORS запросы

**Пример настройки CORS:**
```javascript
const corsOptions = {
  origin: [
    'https://elena-manicure.ru',
    'https://www.elena-manicure.ru',
    'https://your-username.github.io' // для тестирования
  ],
  credentials: true,
  methods: ['GET', 'POST', 'DELETE', 'OPTIONS'],
  allowedHeaders: ['Content-Type', 'x-admin-token']
};
app.use(cors(corsOptions));
```

### 2.6 Защита от двойных бронирований

**Для PostgreSQL (Supabase):**
- [ ] Использовать транзакции с блокировкой строк
- [ ] Использовать `SELECT FOR UPDATE` для блокировки слота
- [ ] Обработать ошибки уникальности (UNIQUE constraint)

**Для файловой системы:**
- [ ] Использовать файловые lock'и или in-memory lock'и
- [ ] Проверять существование перед сохранением
- [ ] Обработать race conditions

### 2.7 Улучшение безопасности админ-токена (опционально)
- [ ] **Вариант 1:** Оставить простой токен (текущая реализация работает)
- [ ] **Вариант 2:** Использовать HMAC подпись для повышения безопасности
- [ ] Если выбираем HMAC: генерировать подпись на основе timestamp + секрета
- [ ] Если выбираем HMAC: проверять подпись на сервере
- [ ] Если выбираем HMAC: добавить проверку времени (защита от replay атак)

**Примечание:** Текущая реализация с простым токеном достаточна для небольшого проекта. HMAC добавляет дополнительный слой безопасности, но усложняет систему. Можно реализовать позже.

**Пример HMAC подписи:**
```javascript
const crypto = require('crypto');
const SECRET = process.env.ADMIN_SECRET;

function generateAdminToken() {
  const timestamp = Date.now();
  const message = `${timestamp}:admin`;
  const signature = crypto.createHmac('sha256', SECRET)
    .update(message)
    .digest('hex');
  return `${timestamp}:${signature}`;
}

function verifyAdminToken(token) {
  const [timestamp, signature] = token.split(':');
  const message = `${timestamp}:admin`;
  const expected = crypto.createHmac('sha256', SECRET)
    .update(message)
    .digest('hex');
  return signature === expected && (Date.now() - parseInt(timestamp)) < 3600000; // 1 час
}
```

### 2.8 Перенос Telegram логики на бэкенд (рекомендуется, но опционально)

**Текущая ситуация:** 
- Telegram отправка происходит с фронтенда (`Booking.tsx`, строка 429)
- Токен передается через переменные окружения `VITE_TELEGRAM_BOT_TOKEN` и `VITE_TELEGRAM_CHAT_ID`
- Токен попадает в собранный JavaScript (виден в браузере, но не в исходниках)

**Вариант 1: Перенести на бэкенд (рекомендуется для безопасности)**
- [ ] Создать эндпоинт `POST /api/send-telegram` на бэкенде
- [ ] Переместить логику отправки в Telegram из `Booking.tsx` на бэкенд
- [ ] Использовать переменные окружения `TELEGRAM_BOT_TOKEN` и `TELEGRAM_CHAT_ID` на бэкенде
- [ ] Обновить фронтенд - вызывать новый эндпоинт после успешного бронирования
- [ ] Убрать `VITE_TELEGRAM_BOT_TOKEN` и `VITE_TELEGRAM_CHAT_ID` с фронтенда
- [ ] Добавить валидацию данных перед отправкой в Telegram

**Вариант 2: Оставить на фронтенде (проще, но менее безопасно)**
- [ ] Оставить текущую реализацию
- [ ] Убедиться, что токен не попадает в публичный репозиторий (использовать GitHub Secrets)
- [ ] Понимать, что токен будет виден в собранном JavaScript (но это приемлемо для многих проектов)
- [ ] Использовать GitHub Secrets для переменных окружения в GitHub Actions

**Рекомендация:** Для начала можно оставить на фронтенде (быстрее миграция), затем перенести на бэкенд при следующем обновлении.

### 2.9 Тестирование локально

**Для Supabase:**
- [ ] Создать проект на https://supabase.com
- [ ] Получить `SUPABASE_URL` и `SUPABASE_KEY`
- [ ] Создать таблицы через SQL Editor
- [ ] Настроить `.env` файл с ключами Supabase
- [ ] Протестировать все функции локально

**Для Railway/Render/Fly.io:**
- [ ] Протестировать локально с файловой системой
- [ ] Убедиться, что папка `data` создается автоматически

---

## 🌐 Этап 3: Настройка GitHub Pages для фронтенда

### 3.1 Подготовка проекта
- [ ] Убедиться, что `vite.config.ts` настроен правильно
- [ ] **Критично:** Проверить `base` в конфиге:
  - Если репозиторий называется `EM` или не `username.github.io`, нужно добавить `base: '/EM/'` (или имя репозитория)
  - Если репозиторий называется `username.github.io`, `base: '/'` (по умолчанию)
- [ ] Проверить, что все пути к ресурсам работают с указанным `base`
- [ ] Создать скрипт для деплоя (опционально)

**Пример для репозитория `EM`:**
```typescript
// vite.config.ts
export default defineConfig({
  base: '/EM/', // Имя репозитория (важно для GitHub Pages)
  // ... остальная конфигурация
});
```

**Проверка base path:**
- После сборки проверить, что все пути начинаются с `/EM/`
- Если используете кастомный домен, можно вернуть `base: '/'` после настройки домена

### 3.2 Настройка GitHub Actions (автоматический деплой)
- [ ] Создать `.github/workflows/deploy.yml` (можно использовать `.github/workflows/deploy.yml.example` как основу)
- [ ] Настроить автоматическую сборку и деплой при push в main
- [ ] Настроить переменные окружения для сборки через GitHub Secrets:
  - `VITE_API_URL` - URL бэкенда на Render/Railway/Fly
  - `VITE_ADMIN_LOGIN` - логин админа
  - `VITE_ADMIN_PASSWORD` - пароль админа
  - `VITE_ADMIN_TOKEN` - токен админа (если используется на фронтенде)
- [ ] Убедиться, что workflow имеет права на запись в Pages

**Важно:**
- `VITE_API_URL` должен указывать на URL бэкенда (например: `https://xxxxx.onrender.com/api`)
- Не коммитить `.env` файлы с секретами
- Все секреты хранить в GitHub Secrets (Settings → Secrets and variables → Actions)
- После настройки Secrets, обновить workflow файл для использования `${{ secrets.VITE_API_URL }}`

**Пример настройки Secrets:**
1. GitHub → Settings → Secrets and variables → Actions
2. New repository secret
3. Name: `VITE_API_URL`, Value: `https://xxxxx.onrender.com/api`
4. Повторить для всех переменных

### 3.3 Альтернатива: Ручной деплой
- [ ] Собрать проект: `npm run build`
- [ ] Переключиться на ветку `gh-pages`
- [ ] Скопировать содержимое `dist/` в корень
- [ ] Закоммитить и запушить

### 3.4 Настройка GitHub Pages
- [ ] Зайти в Settings → Pages репозитория
- [ ] Выбрать источник: GitHub Actions или ветка `gh-pages`
- [ ] Настроить кастомный домен (пока без домена)

---

## 🚀 Этап 4: Деплой бэкенда

### 4.1 Вариант A: Render (РЕКОМЕНДУЕТСЯ — полностью бесплатно)

**Регистрация и деплой:**
1. Зарегистрироваться на https://render.com (карта не нужна)
2. New → Web Service
3. Подключить GitHub репозиторий
4. Настройки:
   - Root Directory: `server`
   - Build Command: `npm install`
   - Start Command: `node index.js`
   - Environment: Node

**Настройка переменных окружения:**
- Environment → Add Environment Variable
- `ADMIN_TOKEN` - токен для админ-панели
- `SUPABASE_URL` - если используете Supabase
- `SUPABASE_KEY` - если используете Supabase
- `PORT` - Render устанавливает автоматически (используйте `process.env.PORT`)

**Важно:** Бесплатный план засыпает после 15 минут бездействия. Первый запрос после пробуждения займет ~30 секунд.

**Получение URL:**
- URL будет вида `https://xxxxx.onrender.com`
- Обновить `VITE_API_URL` в GitHub Secrets (`https://xxxxx.onrender.com/api`)

### 4.2 Вариант B: Railway

**Регистрация и деплой:**
1. Зарегистрироваться на https://railway.app
2. Подключить GitHub репозиторий
3. Создать новый проект → Deploy from GitHub repo
4. Выбрать папку `server` как root directory
5. Railway автоматически определит Node.js и задеплоит

**Настройка переменных окружения:**
- Settings → Variables → Add Variable
- `ADMIN_TOKEN` - токен для админ-панели
- `SUPABASE_URL` - если используете Supabase
- `SUPABASE_KEY` - если используете Supabase
- `PORT` - Railway устанавливает автоматически

**Получение URL:**
- Settings → Domains → Generate Domain
- URL будет вида `https://xxxxx.up.railway.app`

### 4.3 Вариант C: Fly.io

**Регистрация и установка:**
1. Зарегистрироваться на https://fly.io
2. Установить CLI: `curl -L https://fly.io/install.sh | sh`
3. Авторизоваться: `fly auth login`

**Деплой:**
1. В папке `server`: `fly launch`
2. Следовать инструкциям
3. `fly deploy` для деплоя

### 4.4 Настройка Supabase (если используете)

1. Зарегистрироваться на https://supabase.com
2. Создать новый проект
3. SQL Editor → выполнить SQL для создания таблиц
4. Settings → API → скопировать `URL` и `anon key`
5. Добавить в переменные окружения бэкенда

### 4.5 Тестирование API
- [ ] Проверить доступность API: `curl https://xxxxx.onrender.com/api/health` (или Railway/Fly URL)
- [ ] Протестировать все эндпоинты
- [ ] Проверить работу с данными

---

## 🔗 Этап 5: Настройка домена elena-manicure.ru

### 5.1 Покупка домена
- [ ] Купить домен elena-manicure.ru (REG.RU, Timeweb и т.д.)
- [ ] Дождаться активации домена

### 5.2 Настройка DNS для GitHub Pages
- [ ] В настройках репозитория GitHub → Settings → Pages → Custom domain
- [ ] Указать: `elena-manicure.ru`
- [ ] Включить опцию "Enforce HTTPS"
- [ ] Дождаться проверки домена GitHub
- [ ] Получить инструкции по настройке DNS (GitHub покажет нужные записи)

**DNS записи для GitHub Pages (актуальные на 2025):**
```
Для apex домена (elena-manicure.ru):
A записи (4 записи):
185.199.108.153
185.199.109.153
185.199.110.153
185.199.111.153

Для поддомена www (www.elena-manicure.ru):
CNAME запись:
www -> ваш-username.github.io
или
www -> elena-manicure.github.io (если репозиторий так называется)

Альтернатива (рекомендуется):
Использовать только CNAME для apex через провайдера DNS, который поддерживает ALIAS/ANAME записи
```

**Важно:**
- IP адреса GitHub Pages могут измениться, лучше использовать CNAME если возможно
- После настройки DNS, дождаться распространения (до 48 часов, обычно 1-2 часа)
- Проверить DNS: `dig elena-manicure.ru` или через онлайн-сервисы

### 5.3 Настройка DNS для бэкенда (опционально)

**Railway:**
- Settings → Domains → Add Custom Domain
- Настроить CNAME: `api.elena-manicure.ru -> xxxxx.up.railway.app`

**Render:**
- Settings → Custom Domains → Add Custom Domain
- Настроить CNAME: `api.elena-manicure.ru -> xxxxx.onrender.com`

**Fly.io:**
- `fly domains add api.elena-manicure.ru`
- Настроить DNS записи согласно инструкциям

### 5.4 Обновление переменных окружения
- [ ] Обновить `VITE_API_URL` во фронтенде:
  - Railway: `https://xxxxx.up.railway.app/api` или `https://api.elena-manicure.ru/api`
  - Render: `https://xxxxx.onrender.com/api` или `https://api.elena-manicure.ru/api`
  - Fly.io: `https://xxxxx.fly.dev/api` или `https://api.elena-manicure.ru/api`
- [ ] Пересобрать и задеплоить фронтенд

---

## 🧪 Этап 6: Тестирование и проверка

### 6.1 Функциональное тестирование
- [ ] Проверить загрузку главной страницы
- [ ] Проверить календарь бронирования
- [ ] Проверить форму записи
- [ ] Проверить отправку в Telegram
- [ ] Проверить админ-панель
- [ ] Проверить управление рабочими днями
- [ ] Проверить управление слотами

### 6.2 Тестирование на разных устройствах
- [ ] Десктоп
- [ ] Планшет
- [ ] Мобильный телефон
- [ ] Разные браузеры

### 6.3 Проверка производительности
- [ ] Скорость загрузки страниц
- [ ] Время отклика API
- [ ] Работа на медленном интернете

### 6.4 Проверка безопасности
- [ ] HTTPS работает везде
- [ ] Переменные окружения не видны в коде
- [ ] Админ-токен защищен (HMAC подпись работает)
- [ ] Валидация данных работает
- [ ] CORS настроен правильно (только разрешенные домены)
- [ ] Lock механизм защищает от двойных бронирований
- [ ] Нет утечек чувствительных данных в логах

---

## 📝 Этап 7: Миграция данных (опционально, если есть данные)

**Примечание:** Если на Render была только тестовая версия без реальных данных, этот этап можно пропустить.

### 7.1 Проверка наличия данных
- [ ] Проверить, есть ли реальные данные на Render, которые нужно сохранить
- [ ] Если данных нет - пропустить этот этап и перейти к Этапу 8

### 7.2 Экспорт данных (только если есть данные)
- [ ] Экспортировать данные с Render через API:
  - `bookedSlots.json` - через GET `https://your-app.onrender.com/api/booked-slots`
  - `workingDays.json` - через GET `https://your-app.onrender.com/api/working-days`
- [ ] Сохранить резервную копию локально

### 7.3 Импорт в Supabase (только если есть данные)
- [ ] Создать простой скрипт для импорта (если нужно)
- [ ] Загрузить данные в Supabase через `supabase-js` или SQL insert
- [ ] Проверить корректность данных

**Если данных нет:** Supabase таблицы инициализируются пустыми, новые записи появятся при использовании.

---

## 🔄 Этап 8: Переключение и откат (если нужно)

### 8.1 Финальная проверка перед переключением
- [ ] Все работает на новой инфраструктуре (Render + Supabase + GitHub Pages)
- [ ] Домен настроен и работает (если уже настроен)
- [ ] Нет критических ошибок в логах
- [ ] Протестированы все основные функции
- [ ] Данные мигрированы и проверены
- [ ] Резервная копия данных создана

### 8.2 План отката (на случай проблем)
- [ ] Сохранить резервную копию данных из Supabase перед переключением
- [ ] Документировать шаги для возврата на прежний хостинг (если нужно)
- [ ] Подготовить скрипт для экспорта данных из Supabase обратно в JSON
- [ ] Убедиться, что Render/Railway сервисы еще работают (не удалять сразу)

**Пример экспорта из Supabase (упрощенно):**
```javascript
// server/export-data.js
const { createClient } = require('@supabase/supabase-js');
const fs = require('fs').promises;

const supabase = createClient(process.env.SUPABASE_URL, process.env.SUPABASE_KEY);

async function exportData() {
  const { data: slots } = await supabase.from('booked_slots').select('*');
  await fs.writeFile('bookedSlots_backup.json', JSON.stringify({ bookedSlots: slots || [] }, null, 2));

  const { data: days } = await supabase.from('working_days').select('*');
  await fs.writeFile('workingDays_backup.json', JSON.stringify({ overrides: days || [] }, null, 2));

  console.log('Экспорт завершен!');
}
```

### 8.3 Переключение трафика (если используете домен)
- [ ] Обновить DNS записи на новый хостинг
- [ ] Дождаться распространения DNS (до 48 часов, обычно 1-2 часа)
- [ ] Проверить доступность сайта: `curl https://elena-manicure.ru`
- [ ] Проверить работу API: `curl https://xxxxx.onrender.com/api/health`
- [ ] Мониторить логи на наличие ошибок первые 24 часа

### 8.4 Отключение старого хостинга (только после полной проверки)
- [ ] Убедиться, что все работает на новой схеме минимум 1 неделю
- [ ] Убедиться, что нет жалоб от пользователей
- [ ] Создать финальную резервную копию данных
- [ ] Отключить старые сервисы (но не удалять сразу)
- [ ] Через месяц после успешной работы — удалить старый проект (опционально)

---

## 📚 Дополнительные материалы

### Полезные ссылки
- [Railway Documentation](https://docs.railway.app/)
- [Render Documentation](https://render.com/docs)
- [Fly.io Documentation](https://fly.io/docs/)
- [Supabase Documentation](https://supabase.com/docs)
- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)

### Команды для работы
```bash
# Railway (через веб-интерфейс или CLI)
railway login
railway link
railway up

# Render (через веб-интерфейс)
# Деплой автоматический при push в GitHub

# Fly.io
fly auth login
fly launch
fly deploy
fly logs

# Supabase
# Работа через веб-интерфейс или REST API

# GitHub Pages
npm run build
git push origin main  # Автоматический деплой через Actions
```

### Структура проекта после миграции
```
elena-manicure/
├── .github/
│   └── workflows/
│       └── deploy.yml          # Автоматический деплой
├── server/
│   ├── index.js               # Адаптирован под Supabase
│   ├── dbStorage.js           # Модуль для Supabase (нужно создать)
│   └── package.json
├── src/                        # Frontend
├── dist/                       # Собранный фронтенд (для GitHub Pages)
└── vite.config.ts
```

---

## ⚠️ Важные замечания

### Ограничения платформ (бесплатные планы)

**Render:**
- Бесплатный план засыпает после 15 минут бездействия
- Первый запрос после пробуждения занимает ~30 секунд
- Подходит для проектов с низкой нагрузкой, карта не требуется

**Railway:**
- $5 бесплатных кредитов в месяц (обычно хватает для небольшого проекта)
- После исчерпания кредитов сервис останавливается, возможен запрос карты
- Плюс: нет спящего режима

**Fly.io:**
- 3 shared-cpu-1x VM бесплатно
- 3GB persistent volume storage
- 160GB outbound data transfer

**Supabase:**
- 500MB база данных
- 2GB bandwidth
- 50,000 monthly active users
- Подходит для небольших проектов

### Ограничения GitHub Pages
- Максимальный размер репозитория: 1GB
- Максимальный размер файла: 100MB
- Лимит трафика: 100GB/месяц (для бесплатного плана)

### Резервное копирование
- [ ] Настроить автоматическое резервное копирование данных из Supabase/Neon
- [ ] Создать скрипт для регулярного экспорта (можно запускать через cron или GitHub Actions)
- [ ] Хранить резервные копии локально или в облаке (GitHub, Google Drive и т.д.)
- [ ] Рекомендуемая частота: раз в неделю или при значительных изменениях

**Пример скрипта автоматического бэкапа:**
```javascript
// server/backup.js - можно запускать через cron или GitHub Actions
const { createClient } = require('@supabase/supabase-js');
// ... экспорт всех данных в JSON файлы
// ... загрузка в облачное хранилище или коммит в отдельный репозиторий
```

---

## 🎯 Чеклист готовности к миграции

### Перед началом миграции
- [ ] Проект работает локально
- [ ] Все функции протестированы локально
- [ ] Создана резервная копия данных (если есть)
- [ ] Создан аккаунт Supabase и доступен Render/Railway из региона

### После адаптации кода
- [ ] Код адаптирован под Supabase (или выбранную БД)
- [ ] Все функции работают с базой (загрузка, сохранение, удаление)
- [ ] Пагинация реализована для `loadBookedSlots()`
- [ ] Lock механизм работает (если реализован)
- [ ] CORS настроен правильно
- [ ] Telegram логика решена (на фронтенде или бэкенде)

### После деплоя
- [ ] GitHub Pages настроен и работает
- [ ] Бэкенд на Render/Railway/Fly задеплоен
- [ ] API доступен и отвечает на запросы
- [ ] Все переменные окружения настроены
- [ ] Данные мигрированы (если были, иначе пропустить)
- [ ] Финальное тестирование пройдено на продакшене

### После настройки домена
- [ ] Домен куплен и настроен
- [ ] DNS записи настроены правильно
- [ ] HTTPS работает
- [ ] Сайт доступен по домену
- [ ] API работает (если настроен поддомен)

### Финальная проверка
- [ ] Все функции работают на продакшене
- [ ] Нет критических ошибок в логах
- [ ] Резервное копирование настроено
- [ ] Документация обновлена

---

## 📞 Поддержка и отладка

Если возникнут проблемы:

### Проверка логов
1. **Render:** вкладка Logs сервиса
2. **Railway/Fly:** CLI/веб-интерфейс (`railway logs`, `fly logs`)
3. **GitHub Actions:** вкладка Actions в репозитории
4. **Браузер:** консоль разработчика (F12)
5. **Network:** вкладка Network в DevTools для проверки API запросов

### Частые проблемы и решения

**Проблема:** CORS ошибки
- **Решение:** Проверить настройки CORS в `server/index.js`, убедиться что домен добавлен в `origin`

**Проблема:** 429 ошибка от БД или API (слишком много запросов)
- **Решение:** Добавить задержки между запросами, использовать кэширование, оптимизировать количество запросов

**Проблема:** Данные не загружаются (пустой массив)
- **Решение:** Проверить подключение к базе данных (Supabase) или файлам, проверить переменные окружения

**Проблема:** GitHub Pages показывает 404
- **Решение:** Проверить `base` в `vite.config.ts`, убедиться что путь соответствует имени репозитория

**Проблема:** Домен не работает
- **Решение:** Проверить DNS записи через `dig` или онлайн-сервисы, дождаться распространения DNS (до 48 часов)

### Полезные команды для отладки
```bash
# Railway
railway logs                # Смотреть логи
railway status              # Статус сервиса

# Render
# Логи доступны в веб-интерфейсе

# Fly.io
fly logs                    # Смотреть логи
fly status                  # Статус приложения

# Supabase
# Логи доступны в веб-интерфейсе → Logs

# Локальное тестирование
cd server && node index.js  # Запуск сервера локально
curl http://localhost:3001/api/health  # Проверка API

# GitHub Pages
npm run build               # Локальная сборка
npm run preview             # Предпросмотр собранного проекта
```

---

**Дата создания:** 2025-01-XX  
**Дата последнего обновления:** 2025-01-XX  
**Статус:** Готов к выполнению  
**Оценка времени:** 8-12 часов работы (с учетом тестирования и отладки)

**Разбивка по этапам:**
- Этап 1 (Подготовка): 1-2 часа
- Этап 2 (Адаптация бэкенда): 3-4 часа
- Этап 3 (GitHub Pages): 1-2 часа
- Этап 4 (Деплой бэкенда): 1 час
- Этап 5 (Домен): 1-2 часа (зависит от провайдера DNS)
- Этап 6 (Тестирование): 1-2 часа
- Этап 7 (Миграция данных): пропустить (данных нет)
- Этап 8 (Переключение): 0.5-1 час
