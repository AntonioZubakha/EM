# 🌐 Роадмап переноса на домен elena-manicure.ru

Этот чек‑лист для момента, когда домен уже куплен.

## 1) GitHub Pages → elena-manicure.ru
1. В панели DNS у регистратора:
   - Apex (elena-manicure.ru): если есть ALIAS/ANAME — укажите на `antoniozubakha.github.io`. Если нет, создайте A-записи на IP GitHub Pages: `185.199.108.153`, `185.199.109.153`, `185.199.110.153`, `185.199.111.153`.
   - `www`: CNAME → `antoniozubakha.github.io`.
2. В GitHub: Settings → Pages → Custom domain → ввести `elena-manicure.ru`, включить Enforce HTTPS.
3. Подождать SSL (обычно 5–30 мин) и DNS (до 1–2 ч). Проверить `https://elena-manicure.ru` и `https://www.elena-manicure.ru`.
4. После привязки домена можно вернуть `base: '/'` в `vite.config.ts` (не обязательно, `/EM/` тоже работает, но с доменом проще `/`). Если меняете base:
   - поправить `vite.config.ts` → `base: '/'`;
   - закоммитить и дождаться деплоя Pages.

## 2) API → api.elena-manicure.ru (Render)
1. В Render: Settings → Custom Domains → Add `api.elena-manicure.ru`.
2. В DNS: CNAME `api` → хост Render (например `elena-manicure-api.onrender.com`).
3. Дождаться валидации, включить HTTPS (Render выдаст сертификат автоматически).
4. В GitHub Actions Secrets обновить `VITE_API_URL` на `https://api.elena-manicure.ru/api`.
5. Запустить деплой фронта (push в `main` или Re-run workflow). Проверить `curl https://api.elena-manicure.ru/api/health`.

## 3) Проверка после переключения
- Открывается `https://elena-manicure.ru/`, картинки и стили грузятся, навигация работает.
- Записи/форма: запросы идут на `https://api.elena-manicure.ru/api`.
- Админ-операции работают с `x-admin-token`.
- SSL везде зелёный, без смешанного контента.
- DNS: `dig elena-manicure.ru` и `dig www.elena-manicure.ru` показывают нужные записи; `dig api.elena-manicure.ru CNAME` указывает на Render.

## 4) Что делать при проблемах
- Если страница белая: проверить, что base совпадает с доменом (`/`), и что Pages деплой свежий.
- Если 404/старый контент: очистить кэш (Ctrl+F5), убедиться, что Pages деплой успешен.
- Если API не отвечает: проверить CNAME и HTTPS в Render, логи Render.

## 5) Гигиена и напоминания
- Обновить `VITE_API_URL` при смене домена API.
- Следить за сроками продления домена и автопродлением.
- Держать Render переменные (`SUPABASE_URL`, `SUPABASE_KEY`, `ADMIN_TOKEN`) актуальными.

