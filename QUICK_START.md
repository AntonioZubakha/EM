# ⚡ Быстрый старт - Система автоматического бронирования

## Как это работает

1. **Клиент заполняет форму** → отправляет заявку в Telegram
2. **Система автоматически резервирует слот** → сохраняет в `server/data/bookedSlots.json`
3. **Другие клиенты видят занятые слоты** → не могут забронировать то же время

## Запуск для разработки

### Шаг 1: Установка зависимостей

```bash
# Frontend
npm install

# Backend
cd server
npm install
cd ..
```

### Шаг 2: Настройка .env

Создайте файл `.env` в корне проекта:

```env
VITE_TELEGRAM_BOT_TOKEN=your_bot_token_here
VITE_TELEGRAM_CHAT_ID=your_chat_id_here
VITE_API_URL=http://localhost:3001/api
```

### Шаг 3: Запуск

**Терминал 1 (Frontend):**
```bash
npm run dev
```

**Терминал 2 (Backend):**
```bash
npm run server
```

### Шаг 4: Проверка

- Откройте http://localhost:3050
- Попробуйте забронировать время
- Проверьте, что слот автоматически стал недоступным

## Что происходит при бронировании

1. Клиент выбирает дату и время
2. Заполняет форму (имя, телефон, услуга)
3. Нажимает "Отправить"
4. **Автоматически:**
   - Отправляется сообщение в Telegram
   - Слот резервируется на сервере
   - Слот становится недоступным для других клиентов
   - Данные сохраняются в `server/data/bookedSlots.json`

## Структура данных

Файл `server/data/bookedSlots.json`:
```json
{
  "bookedSlots": [
    {
      "date": "2025-01-07",
      "time": "14:00",
      "name": "Иван Иванов",
      "phone": "+79161234567",
      "service": "Маникюр",
      "bookedAt": "2025-01-15T10:00:00.000Z"
    }
  ]
}
```

## Деплой на продакшен

См. подробную инструкцию в `SERVER_DEPLOY.md`

**Кратко:**
1. Frontend → статический хостинг (Nginx/Apache)
2. Backend → Node.js сервер (PM2/systemd)
3. Настройте `VITE_API_URL` на продакшен URL

---

**Готово!** Система работает автоматически 🎉

