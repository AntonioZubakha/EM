# 🚀 Деплой на Render

## Быстрый старт

### Вариант 1: Через веб-интерфейс Render (рекомендуется)

1. **Зарегистрируйтесь/войдите на [Render.com](https://render.com)**

2. **Создайте новый Static Site:**
   - Нажмите "New +" → "Static Site"
   - Или перейдите: https://dashboard.render.com/new/static

3. **Подключите репозиторий:**
   - Если проект в GitHub/GitLab/Bitbucket, подключите репозиторий
   - Или используйте "Deploy from public Git repository"

4. **Настройте параметры сборки:**
   ```
   Name: elena-manicure (или любое другое имя)
   Branch: main (или master, в зависимости от вашей ветки)
   Root Directory: (оставьте пустым)
   Build Command: npm install && npm run build
   Publish Directory: dist
   ```

5. **Добавьте переменные окружения:**
   - Нажмите "Advanced" → "Environment Variables"
   - Добавьте:
     ```
     VITE_TELEGRAM_BOT_TOKEN = ваш_токен_бота
     VITE_TELEGRAM_CHAT_ID = ваш_chat_id
     ```

6. **Нажмите "Create Static Site"**

7. **Дождитесь завершения деплоя** (обычно 2-5 минут)

8. **Ваш сайт будет доступен по адресу:**
   ```
   https://elena-manicure.onrender.com
   ```
   (или по вашему кастомному домену, если настроите)

---

### Вариант 2: Через render.yaml (автоматическая настройка)

Если вы используете файл `render.yaml`:

1. **Убедитесь, что файл `render.yaml` есть в корне проекта**

2. **На Render:**
   - "New +" → "Blueprint"
   - Подключите репозиторий
   - Render автоматически обнаружит `render.yaml` и применит настройки

3. **Добавьте переменные окружения вручную:**
   - Перейдите в настройки сервиса
   - Environment → Add Environment Variable
   - Добавьте `VITE_TELEGRAM_BOT_TOKEN` и `VITE_TELEGRAM_CHAT_ID`

---

## Настройка кастомного домена (опционально)

1. В настройках Static Site → "Custom Domains"
2. Добавьте ваш домен (например, `elena-manicure.ru`)
3. Следуйте инструкциям для настройки DNS записей

---

## Важные моменты

### ✅ Бесплатный план Render:
- Сайт "засыпает" после 15 минут неактивности
- Первая загрузка после "сна" может занять 30-60 секунд
- Подходит для тестирования и временного размещения

### ✅ Переменные окружения:
- **НЕ храните** токены в коде!
- Используйте Environment Variables в настройках Render
- Токены будут доступны только во время сборки

### ✅ SPA роутинг:
- Render автоматически обрабатывает SPA роутинг
- Все запросы перенаправляются на `index.html`

### ✅ HTTPS:
- Render автоматически предоставляет SSL сертификат
- HTTPS включен по умолчанию

---

## Обновление сайта

После каждого push в репозиторий Render автоматически:
1. Обнаружит изменения
2. Запустит новую сборку
3. Задеплоит обновленную версию

---

## Мониторинг

В панели Render вы можете:
- Видеть логи сборки и деплоя
- Отслеживать использование ресурсов
- Настраивать уведомления

---

## Проблемы и решения

### Сайт не собирается:
- Проверьте логи сборки в панели Render
- Убедитесь, что `package.json` содержит все зависимости
- Проверьте, что Node.js версия совместима (Render использует последнюю LTS)

### Переменные окружения не работают:
- Убедитесь, что переменные начинаются с `VITE_`
- Пересоберите сайт после добавления переменных
- Проверьте, что переменные добавлены в правильный сервис

### Изображения не загружаются:
- Убедитесь, что все изображения в папке `public/`
- Проверьте пути к изображениям (должны начинаться с `/`)

---

## Стоимость

- **Бесплатный план:** подходит для тестирования
- **Starter ($7/мес):** для постоянного использования без "сна"
- **Professional:** для продакшена с кастомным доменом

---

**Готово!** Ваш сайт будет доступен на Render 🎉

