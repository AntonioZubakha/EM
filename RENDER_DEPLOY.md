# 🚀 Деплой на Render.com

## Структура деплоя

Проект состоит из двух сервисов на Render:

1. **Frontend** (Static Site) - статический сайт React
2. **Backend** (Web Service) - API сервер Node.js

## Пошаговая инструкция

### Шаг 1: Подготовка репозитория

1. Убедитесь, что все изменения закоммичены и запушены в GitHub
2. Проверьте, что файл `render.yaml` находится в корне репозитория

### Шаг 2: Создание сервисов на Render

#### Вариант A: Автоматический деплой через render.yaml (рекомендуется)

1. Зайдите на [Render Dashboard](https://dashboard.render.com)
2. Нажмите **"New"** → **"Blueprint"**
3. Подключите ваш GitHub репозиторий
4. Render автоматически обнаружит `render.yaml` и создаст оба сервиса

#### Вариант B: Ручное создание сервисов

**Frontend сервис:**

1. **New** → **Static Site**
2. Подключите GitHub репозиторий
3. Настройки:
   - **Name:** `elena-manicure-frontend`
   - **Build Command:** `npm install && npm run build`
   - **Publish Directory:** `dist`
   - **Environment Variables:**
     ```
     VITE_TELEGRAM_BOT_TOKEN=your_bot_token
     VITE_TELEGRAM_CHAT_ID=your_chat_id
     VITE_API_URL=https://elena-manicure-api.onrender.com/api
     ```
   - **Redirects/Rewrites:** Добавьте правило:
     ```
     /*    /index.html   200
     ```

**Backend сервис:**

1. **New** → **Web Service**
2. Подключите тот же GitHub репозиторий
3. Настройки:
   - **Name:** `elena-manicure-api`
   - **Root Directory:** `server` (опционально, можно оставить пустым)
   - **Environment:** `Node`
   - **Build Command:** `cd server && npm install`
   - **Start Command:** `cd server && node index.js`
   - **Environment Variables:**
     ```
     NODE_ENV=production
     ```
   - **Health Check Path:** `/api/health`

### Шаг 3: Настройка переменных окружения

**Важно:** После создания backend сервиса, обновите `VITE_API_URL` во frontend сервисе:

1. Зайдите в настройки frontend сервиса
2. Перейдите в **Environment**
3. Обновите `VITE_API_URL` на URL вашего backend сервиса:
   ```
   https://elena-manicure-api.onrender.com/api
   ```
4. Нажмите **Save Changes** → Render пересоберет frontend

### Шаг 4: Проверка работы

1. **Frontend URL:** `https://elena-manicure-frontend.onrender.com`
2. **Backend URL:** `https://elena-manicure-api.onrender.com`
3. **Health Check:** `https://elena-manicure-api.onrender.com/api/health`

## Важные моменты

### 1. Порядок деплоя

1. Сначала деплойте **Backend** сервис
2. Получите его URL
3. Затем деплойте **Frontend** с правильным `VITE_API_URL`

### 2. Переменные окружения

**Frontend (.env или в настройках Render):**
```env
VITE_TELEGRAM_BOT_TOKEN=your_bot_token
VITE_TELEGRAM_CHAT_ID=your_chat_id
VITE_API_URL=https://elena-manicure-api.onrender.com/api
```

**Backend:**
```env
NODE_ENV=production
PORT=10000  # Render автоматически устанавливает, но можно указать явно
```

### 3. CORS

Backend уже настроен с CORS, поэтому он будет работать с frontend на Render.

### 4. Хранение данных

Данные о записях хранятся в `server/data/bookedSlots.json`.

⚠️ **Важно:** На Render файловая система эфемерная. При перезапуске сервиса данные могут потеряться.

**Решения:**
- Используйте Render Disk (платная функция)
- Или настройте внешнее хранилище (MongoDB, PostgreSQL, Redis)
- Или используйте Render для тестирования, а на продакшен деплойте на VPS

### 5. Автоматический деплой

Render автоматически деплоит при каждом push в main ветку.

### 6. Логи

Просмотр логов:
- Frontend: **Logs** в настройках сервиса
- Backend: **Logs** в настройках сервиса

## Обновление после изменений

1. Сделайте изменения в коде
2. Закоммитьте и запушьте в GitHub
3. Render автоматически пересоберет и перезапустит сервисы

## Тестирование

1. Откройте frontend URL
2. Попробуйте забронировать время
3. Проверьте, что:
   - Форма отправляется в Telegram
   - Слот автоматически резервируется
   - Занятые слоты отображаются корректно

## Troubleshooting

### Frontend не видит API

1. Проверьте `VITE_API_URL` в настройках frontend
2. Убедитесь, что backend сервис запущен
3. Проверьте CORS настройки в backend

### Backend не запускается

1. Проверьте логи в Render Dashboard
2. Убедитесь, что `server/package.json` содержит все зависимости
3. Проверьте, что `server/index.js` существует

### Данные теряются

Это нормально для Render (эфемерная файловая система). Для продакшена используйте:
- VPS с постоянным хранилищем
- Базу данных (MongoDB, PostgreSQL)
- Render Disk (платно)

---

**Готово!** Ваш проект готов к тестированию на Render 🎉

